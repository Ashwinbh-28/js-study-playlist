/*
note that: For the increments of the operators like "++x", the value
returns the incremented value for and returns the value for the both as same

But for the "x++" value the increment operator is returned with the value of incremented and then
return the input values  
*/

let x = 3
let y = x++

console.log(a={x}, b={y});